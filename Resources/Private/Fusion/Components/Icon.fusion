prototype(Aim29.Boxicons.Icon:IconComponent) < prototype(Neos.Fusion:Component) {
	icon = ''
	size = ''
	hasBorder = false

	renderer = afx`
		<i class={props.icon + (props.size ? ' bx-' + props.size : '') + (props.hasBorder ? ' bx-border' : '')} aria-hidden="true"></i>
	`
}